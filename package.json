{
  "name": "simple-api",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "NODE_ENV=production bun run src/index.ts",
    "db:generate": "bun run drizzle-kit generate",
    "db:migrate": "bun run drizzle-kit migrate",
    "db:studio": "bun run drizzle-kit studio",
    "test": "bun run tests/test-comprehensive.ts",
    "test:current": "bun run tests/test-current.ts",
    "lint": "echo 'No linter configured yet'",
    "format": "echo 'No formatter configured yet'",
    "build": "echo 'No build process configured yet'"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/bun": "latest",
    "drizzle-kit": "^0.27.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "drizzle-orm": "^0.44.4",
    "elysia": "^1.3.2",
    "mysql2": "^3.14.1"
  }
}